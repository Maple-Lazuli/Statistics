---
title: "L1KC"
author: "Ada Lazuli"
date: "2026-01-17"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
setwd("/home/maple/CodeProjects/Statistics/STAT508/notebooks")
```

```{r}
library(ggplot2)
library(dplyr)
```

# Data Loading

```{r}
df <- read.csv("../sample_data/L01_Insurance_m.csv")
summary(df)
```

```{r}
head(df)
```

```{r}
df %>% filter(sex == "male") %>% group_by(region) %>% summarize(mean_charges = mean(charges, na.rm = T)) %>% arrange(desc(mean_charges))
```